[changelog]
# changelog header
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
"""
# template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}{{ commit.message | upper_first }}
    {% endfor %}
{% endfor %}\n
"""
# remove the leading v from the tag name (e.g. v1.1.0 -> 1.1.0)
trim = true
# changelog footer
footer = """
<!-- generated by git-cliff -->
"""

[git]
# parse the commits based on https://www.conventionalcommits.org
conventional_commits = false
# filter out the commits that are not conventional (e.g. merge commits)
filter_unconventional = false
# process each line of a commit as an individual commit
split_commits = false
# regex for parsing and grouping commits
commit_parsers = [
  { message = "^功能", group = "新功能" },
  { message = "^修复", group = "Bug 修复" },
  { message = "^重构", group = "重构" },
  { message = "^性能", group = "性能优化" },
  { message = "^文档", group = "文档" },
  { message = "^样式", group = "样式" },
  { message = "^测试", group = "测试" },
  { message = "^杂项", group = "杂项" },
  { message = "^构建", group = "构建系统" },
  { message = "^持续集成", group = "持续集成" },
  { message = ".*", group = "其他" },
]
# protect breaking changes from being omitted due to matching messages
protect_breaking_commits = false
# filter out the commits that are not matched by commit parsers
filter_commits = false
# glob pattern for matching git tags
tag_pattern = "v[0-9]*"
# skip the tags that match the glob pattern
skip_tags = ""
# ignore the tags that match the glob pattern
ignore_tags = ""
# sort the commits inside sections by oldest/newest order
sort_commits = "oldest"
